{
  "name": "workspace",
  "version": "0.0.0",
  "license": "MIT",
  "scripts": {
    "dev": "pnpm --filter @workspace/yt-history run dev",
    "build": "pnpm --filter @workspace/yt-history run build",
    "typecheck": "pnpm --filter @workspace/yt-history run typecheck",
    "deploy:pages": "pnpm --filter @workspace/yt-history run deploy"
  },
  "private": true,
  "devDependencies": {
    "prettier": "^3.8.1",
    "typescript": "~5.9.2"
  }
}
